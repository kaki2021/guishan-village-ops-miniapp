/**
 * 小程序本地配置示例。
 *
 * 首次运行前复制为 config.local.js，再填入自己的微信云开发环境 ID：
 *   cp miniprogram/config.example.js miniprogram/config.local.js
 *
 * config.local.js 已加入 .gitignore，不应提交到公开仓库。
 */
module.exports = {
  cloudEnvId: 'your-cloud-env-id'
}
