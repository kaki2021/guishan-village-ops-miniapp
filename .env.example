# 本文件只提供字段示例。复制为 .env 后填写本机值；.env 不会提交到 Git。

# 可选：供 uploadCloudFunction.sh 使用。
# Windows 路径建议在 Git Bash / WSL 中使用正斜杠，并给含空格的值加引号。
WECHAT_CLI_PATH="/path/to/wechat-devtools-cli"
WECHAT_CLOUD_ENV_ID="your-cloud-env-id"

# 可留空；脚本默认使用当前项目根目录。
WECHAT_PROJECT_PATH=""

# 以下是云函数运行时所需字段的说明，不会由本地脚本自动上传。
# 请在微信云开发控制台中为对应云函数配置真实值。
DEEPSEEK_API_KEY=""
ARK_API_KEY=""
ARK_MODEL=""
